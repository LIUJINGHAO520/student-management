<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liu.studentmanagement.mapper.StudentScoreMapper">

    <select id="selectByBlurry" resultType="com.liu.studentmanagement.entity.StudentScore">
        SELECT * FROM student_score
        <where>
            <if test="keyword != null and keyword != ''">
                student_name LIKE CONCAT('%', #{keyword}, '%')
                OR course_name LIKE CONCAT('%', #{keyword}, '%')
                OR teacher_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>